spring:
  cloud:
    config:
      uri: '@configServerPath@'
      name: goods,eureka,datasource,redis,rabbitmq
      profile: '@activeProfile@'

#      discovery:
#        enabled: true
#        service-id: config-server

#    import=configserver: http://localhost:30000